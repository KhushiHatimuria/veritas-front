import { Stack } from "expo-router";
import React from "react";

export default function RootLayout() {
  return (
    <Stack>
      {/* By default, expo-router automatically detects files in /app */}
    </Stack>
  );
}
